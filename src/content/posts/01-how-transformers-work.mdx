---
title: "How Transformers Work"
description: "Every AI model you use today is built on one idea. Here is how it works."
publishDate: 2024-01-07
week: 1
---

import Scene from '../../components/Scene.astro';

<Scene animation="title" index={0}>
  <p class="week-tag animate-in">Week 1</p>
  <h1 class="animate-in">How Transformers Work</h1>
  <p class="animate-in">Every AI model you use today is built on the same foundation. This is it.</p>
</Scene>

<Scene animation="foundation" index={1}>
  <h2 class="animate-in">One idea. Every model.</h2>
  <p class="animate-in">ChatGPT, Claude, Gemini. All of them run on the same architecture.</p>
  <p class="animate-in">It is called the transformer. Understanding it changes how you see everything else.</p>
</Scene>

<Scene animation="tokens" index={2}>
  <h2 class="animate-in">Text becomes numbers</h2>
  <p class="animate-in">Before a model reads your words, it breaks them into pieces called tokens.</p>
  <p class="animate-in">A token is roughly four characters. Models do not think in words. They think in numbers.</p>
</Scene>

<Scene animation="heatmap" index={3}>
  <h2 class="animate-in">Which words matter?</h2>
  <p class="animate-in">For every token, the model asks one question: which other tokens matter right now?</p>
  <p class="animate-in">In "The bank was steep and muddy," the word "bank" attends most to "steep" and "muddy." That is how it knows you mean a riverbank.</p>
  <p class="animate-in">This process is called attention.</p>
</Scene>

<Scene animation="sequential" index={4}>
  <h2 class="animate-in">How models used to work</h2>
  <p class="animate-in">Before transformers, models read text one word at a time, left to right.</p>
  <p class="animate-in">By the time they reached the end of a long sentence, they had forgotten the beginning.</p>
</Scene>

<Scene animation="parallel" index={5}>
  <h2 class="animate-in">Transformers read everything at once</h2>
  <p class="animate-in">Every token can attend to every other token simultaneously.</p>
  <p class="animate-in">That parallel processing is why transformers are faster to train and better at remembering context across long documents.</p>
</Scene>

<Scene animation="text-reveal" index={6}>
  <h2 class="animate-in">Why this matters to you</h2>
  <p class="animate-in">The attention mechanism is why Claude can follow a 10,000-word document.</p>
  <p class="animate-in">The model is not scanning through text. It is weighing relationships across the whole thing at once.</p>
  <p class="animate-in">That is the transformer.</p>
</Scene>

<Scene animation="next" index={7}>
  <p class="week-tag animate-in">Coming up</p>
  <h2 class="animate-in">Week 2: Training LLMs</h2>
  <p class="animate-in">Now you have the architecture. Next week you get the engine. How these models learn from data, and what scale actually means.</p>
</Scene>
